# Credentials — never commit. freechain reads keys from here at runtime.
.env
.env.*
!.env.example

node_modules/
*.log

# Release artifacts — built by scripts/build-release.mjs, published to GitHub
# Releases rather than committed.
dist/

# Private keys and certificates
*.pem
*.key
*.p12
*.pfx
*.jks

# Editor and OS artifacts
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# Local code-intelligence tooling — machine-specific, not part of the project.
.gitnexus/
.claude/
.serena/
CLAUDE.md
AGENTS.md
